name: cpp
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: installing cmake
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake g++
          cd build
          cmake ..
          make
